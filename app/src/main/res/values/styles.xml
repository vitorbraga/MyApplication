<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Material" parent="android:Theme.Material.Light">
        <!-- theme customizations -->
        <!--   your app branding color for the app bar -->
        <item name="android:textColorPrimary">@color/tinder_red</item>
        <item name="android:colorPrimary">@color/white</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/black</item>
        <!--   theme UI controls like checkboxes and text fields -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CollapseToolbarAppearance">
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="TinderActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimaryDark">#cc0986ff</item>
        <item name="colorPrimary">@color/white</item>
        <item name="titleTextColor">@color/tinder_red</item>
        <item name="android:textColorPrimary">@color/tinder_red</item>
        <item name="android:editTextColor">@android:color/background_dark</item>
        <item name="android:textColor">@android:color/background_dark</item>
        <item name="colorControlNormal">@android:color/background_dark</item>
        <item name="colorControlActivated">#ff0043c5</item>
        <item name="colorControlHighlight">#ffcb6345</item>
    </style>
</resources>
